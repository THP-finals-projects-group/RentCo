<% flash.each do |type, msg| %>
  <div class="alert alert-<%= type %> mb-3", style="vertical-align:top;">
    <%= msg %>
    <button type="button" class="close" data-dismiss="alert" aria-label="Close">
      <span aria-hidden="true">x</span>
    </button>
  </div>
<% end %>
